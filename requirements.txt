# Pinned dependencies for inference and rendering.
# Tested with Python 3.10/3.11.
#
# For a CPU-only install, the defaults below work as-is.
# For CUDA, install torch / torchaudio from the matching wheel index.
# (e.g. for CUDA 12.1: pip install torch==2.4.1+cu121 torchaudio==2.4.1+cu121
#       --index-url https://download.pytorch.org/whl/cu121)

torch==2.4.1
torchaudio==2.4.1
numpy==1.26.4
librosa==0.9.2
soundfile==0.12.1
pretty_midi==0.2.10
# Required only for render mode 4 (FX + LUFS norm). Not needed for mode 1
# or for transcribe.py.
pedalboard==0.8.9
pyloudnorm==0.2.0
